# 项目概览

---

## 定位

**FP** 是一个 CLI/WebUI/ACP 多模式交互式 AI Agent，具备自我迭代的能力。项目设计灵感部分来自游戏 *Rain World* 中的迭代器角色。

核心特性：

- **LLM 对话** — 通过自实现的 OpenAI HTTP 客户端与大语言模型交互，无需官方 SDK
- **工具调用** — 支持 Function Calling 循环执行，Agent 自主决定调用时机
- **插件系统** — 生命周期钩子驱动，支持 observe/transform 两种模式，可拦截/修改/守卫流程
- **会话管理** — 基于 JSONL 文件的持久化方案，支持多会话切换、续聊、摘要生成
- **自我修改** — Agent 可以修改自身源代码并测试验证，在安全前提下突破常规
- **多模式支持** — 通过 `--mode` 参数在 CLI / WebUI / ACP 三种入口间切换

设计哲学：

> *"我们都是迷宫里的虫子，通过反复试错逼近最优解。"*

---

## 技术栈

| 依赖 | 版本要求 | 用途 |
|------|----------|------|
| Python | ≥3.10 | 运行环境，全异步 |
| asyncio | 标准库 | 异步事件循环 |
| httpx | ≥0.24 | HTTP 客户端，替代 openai SDK |
| prompt_toolkit | ≥3.0 | CLI 交互增强（历史、补全、多行输入） |
| fastapi + uvicorn | — | WebUI 模式（可选安装 `fp[webui]`） |
| websockets | — | WebUI WebSocket 通信 |
| rich | 可选 | 富文本输出（如果安装则自动使用） |
| wcwidth | 可选 | 宽字符宽度计算（中文截断对齐） |

Python 标准库依赖：`json`、`os`、`re`、`sys`、`datetime`、`inspect`、`contextlib`、`collections`、`functools`、`enum`、`argparse`。

**无第三方 LLM SDK 依赖** —— LLM 通信完全通过自实现的 `fp_core/core/llm_client.py` 完成，使用 httpx.AsyncClient 直调 OpenAI 格式 API。

---

## 快速启动

```bash
# 1. 安装根包（自动安装 fp, fp-core, fp-terminal）
pip install -e .

# 2. 安装 WebUI 支持（可选）
pip install -e ".[webui]"

# 3. 安装 ACP 支持（可选）
pip install -e ".[acp]"

# 4. 配置 API Key
#    方式 A：修改 ~/.config/fp/config.json
#    方式 B：设置环境变量
export LLM_API_KEY="sk-your-key-here"

# 5. 运行（CLI 模式）
python -m fp

#    WebUI 模式
python -m fp --mode webui

#    ACP 模式
python -m fp --mode acp
```

首次启动时自动生成 `~/.config/fp/config.json`。若不配置 API Key，启动时会给出明确提示。

CLI 支持的内置命令（以 `/` 开头，共 15 个）：

| 命令 | 作用 |
|------|------|
| `/help` (`/?`) | 显示帮助信息 |
| `/exit` (`/quit`) | 退出 Agent（自动保存会话摘要） |
| `/exit!` | 核弹退出（删除当前会话） |
| `/clear` | 清空当前会话上下文 |
| `/new` (`/fresh`) | 新建空白会话（保存旧会话） |
| `/back` | 回退到对话历史某处 |
| `/history` | 查看当前会话历史摘要 |
| `/fork` | 从当前上下文分叉新会话 |
| `/compact` | 压缩对话历史 |
| `/sc` | 短路已完成的连通块 |
| `/resume` | 切换/删除历史会话（`/resume delete` 删除） |
| `/session` | 显示当前会话 ID |
| `/option` (`/opt` `/ext` 等) | 扩展管理（工具/命令/插件启停） |
| `/reload` (`/rl`) | 热重载 Agent |
| `/token` (`/tokens`) | 显示 token 消耗统计 |

---

## 项目目录结构（Monorepo）

```
agent/                              # monorepo 根
├── packages/
│   ├── fp/                         # 路由入口
│   │   └── src/fp/main.py          # argparse → --mode cli/webui/acp
│   │
│   ├── fp-core/                    # 核心引擎
│   │   └── src/fp_core/
│   │       ├── core/               # 核心模块
│   │       │   ├── agent.py        # Agent 主干，编排主循环
│   │       │   ├── lifecycle.py    # LifecycleManager — 15 个钩子
│   │       │   ├── llm_client.py   # 自实现 OpenAI HTTP 客户端
│   │       │   ├── llm_service.py  # LLM 调用封装
│   │       │   ├── session.py      # 会话持久化管理器
│   │       │   ├── conversation.py # 上下文状态管理
│   │       │   ├── prompt_builder.py # 系统提示词构建
│   │       │   ├── tool_executor.py  # 工具调用执行器
│   │       │   └── io.py           # IO 通道抽象（CLI/Web）
│   │       ├── commands/           # 15 个内置命令
│   │       │   └── __init__.py     # CommandRegistry — 命令注册表
│   │       ├── tools/              # 工具系统
│   │       │   ├── __init__.py     # ToolRegistry
│   │       │   ├── core.py         # 核心工具（bash/read_file/write_file/edit_file）
│   │       │   └── extensions/     # 内置工具扩展（插件工具）
│   │       │       ├── web_fetch_plugin.py
│   │       │       ├── python_plugin.py
│   │       │       ├── subagent_plugin.py
│   │       │       ├── memory_read_plugin.py
│   │       │       └── memory_save_plugin.py
│   │       ├── plugins/            # 生命周期插件
│   │       │   └── base/           # Plugin 基类与注册表
│   │       ├── prompts/            # 提示词模板
│   │       ├── config.py           # 配置管理（三级优先级）
│   │       └── display.py          # 显示层
│   │
│   ├── fp-terminal/                     # CLI 交互界面
│   │   └── src/fp_cli/main.py      # prompt_toolkit 主循环
│   │
│   ├── fp-webui/                   # Web 界面
│   │   └── src/fp_webui/main.py    # FastAPI + WebSocket
│   │
│   └── fp-acp/                     # ACP 协议服务
│       └── src/fp_acp/             # 远程 Agent 通信协议
│
├── docs/
│   ├── dev/                        # 开发者文档（本文档所在目录）
│   └── guide/                      # 用户指南
│
├── pyproject.toml                  # 项目元数据与构建配置
├── README.md
└── LICENSE
```

### 关键文件速查表

| 文件 | 职责 |
|------|------|
| `packages/fp/src/fp/main.py` | 入口 — 参数解析 + `--mode` 分发 |
| `packages/fp-terminal/src/fp_cli/main.py` | CLI 主循环，交互式 read-eval-print loop |
| `packages/fp-core/src/fp_core/config.py` | 三级优先级配置管理（JSON > 环境变量 > 默认值） |
| `packages/fp-core/src/fp_core/display.py` | 终端输出样式系统 |
| `packages/fp-core/src/fp_core/core/agent.py` | Agent 主干，持有所有服务实例，编排主循环 |
| `packages/fp-core/src/fp_core/core/lifecycle.py` | 生命周期管理器，15 个钩子点 |
| `packages/fp-core/src/fp_core/core/llm_client.py` | 自实现 OpenAI HTTP 客户端 |
| `packages/fp-core/src/fp_core/core/session.py` | 会话持久化管理器 |
| `packages/fp-core/src/fp_core/tools/__init__.py` | 工具注册表 + 核心工具 + 插件工具动态加载 |

### 数据路径

| 用途 | 路径 |
|------|------|
| 配置文件 | `~/.config/fp/config.json` |
| 会话文件 | `~/.local/share/fp/sessions/*.jsonl` |
| 长期记忆 | `~/.local/share/fp/{private,public,fetched}/memory/*.md` |
| 任务追踪 | `~/.local/share/fp/tasks.json` |

遵循 XDG Base Directory 规范，`$XDG_CONFIG_HOME` / `$XDG_DATA_HOME` 环境变量可覆盖上述路径。

---

## 与 Rain World 的关联

项目的设计灵感来自游戏 *Rain World*：

- **Five Pebbles**（五块卵石）— 游戏中的一个迭代器，拥有庞大的处理能力和独特个性
- **迭代器（Iterator）** — 游戏中的半生物超级计算机，被建造来解决"大问题"（The Great Problem）
- **自我修改** — 游戏中迭代器可以修改自己的代码，本项目的自修改（self-modification）机制正是此概念的工程实现

---

## 架构概览

```
python -m fp
      │
      ▼
  fp/main.py ─── argparse ───┬── --mode cli ──→ fp_cli/main.py ──→ fp_core Agent
                              ├── --mode webui ─→ fp_webui/main.py ─→ fp_core Agent
                              └── --mode acp  ──→ fp_acp/main.py ──→ fp_core Agent
                                                      │
                                                      ▼
                                            ┌─────────────────────┐
                                            │    fp_core Agent    │
                                            │                     │
                                            │  ┌───────────────┐  │
                                            │  │  Lifecycle    │  │
                                            │  │  (15 hooks)   │  │
                                            │  └──────┬────────┘  │
                                            │         │           │
                                            │  ┌──────▼────────┐  │
                                            │  │  ToolExecutor │  │
                                            │  │  ToolRegistry │  │
                                            │  │  + extensions │  │
                                            │  └──────┬────────┘  │
                                            │         │           │
                                            │  ┌──────▼────────┐  │
                                            │  │  LLM Client   │  │
                                            │  │  (httpx)      │  │
                                            │  └───────────────┘  │
                                            └─────────────────────┘
```

三种模式共享同一个 `fp_core` 引擎，仅 IO 通道和交互界面不同，核心 Agent 逻辑完全复用。

---

## 三类扩展对比

FP 提供三种可扩展机制，各有不同定位和加载方式：

| | Plugin | Command | Tool |
|---|---|---|---|
| **中文称谓** | 插件（生命周期钩子） | 命令（斜杠输入） | 工具（函数调用） |
| **触发方式** | 生命周期事件自动触发 | 用户输入 `/xxx` | LLM 主动发起 Function Call |
| **能干什么** | 监听/拦截/修改流程，如通知、审计、守卫 | 用户直接操作的快捷行为 | LLM 自主调用的具体操作 |
| **文件格式** | `.py`，继承 `Plugin` 基类 | `.py`，导出 `name`/`execute` | `.py`，导出 `PLUGIN_DEFINITION`/`execute` |
| **加载目录** | `fp_core/plugins/` + 三来源用户目录 | `fp_core/commands/` + 三来源用户目录 | `fp_core/tools/core.py` + 内置 extensions + 三来源用户目录 |
| **是否影响 LLM 上下文** | ❌ 不影响（observe 型）/ ✅ 可修改（transform 型） | ❌ 仅在终端侧执行 | ✅ 工具结果进入对话上下文 |
| **覆写机制** | 同名插件覆写 | 同名命令覆写 | 同名工具覆写 |
| **热重载** | ✅ 运行时 `scan()` | ✅ 模块重导入 | ✅ 运行时 `_load_plugins()` |
| **是否可用户扩展** | ✅ `~/.local/share/fp/private/plugins/` 等三来源 | ✅ `~/.local/share/fp/private/commands/` 等三来源 | ✅ `~/.local/share/fp/private/tools/extensions/` 等三来源 |

> 三种扩展遵循**统一的文件命名约定**（文件即开关），详见 [文件命名约定.md](文件命名约定.md)。
