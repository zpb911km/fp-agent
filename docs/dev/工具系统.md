# 工具系统 —— 导入、注册与覆写机制（全异步重构版）

工具系统是 Agent 与外部世界交互的桥梁。LLM 通过 Function Calling 声明需要调用的工具，由 `ToolRegistry` 统一调度执行并返回结果。

---

## 架构概览

```
ToolRegistry.__init__()
  ├── _load_core()     → core.py 硬绑定（4 个核心工具）
  └── _load_plugins()  → extensions/ 自动扫描（内置 → 用户）
       ├── _load_from_dir(builtin_dir, "fp_core.tools.extensions")
       └── _load_from_dir(user_dir)  # 同名覆盖
```

**核心原则：**

- **bash, read_file, write_file, edit_file** —— 保持直接绑定（`core.py`），不可插件化
- 其他工具通过 `extensions/*_plugin.py` 扩展机制动态加载

---

## 核心工具（不可插件化）

4 个核心工具在 `tools/core.py` 中定义，**不可通过用户插件卸载或覆盖**。

| 工具名 | 参数 | 说明 |
|--------|------|------|
| `bash` | `command: str, timeout?: int=300, force?: bool=false` | 执行 shell 命令；**显式走系统 bash**（非 `/bin/sh`，支持 `${var:0:6}`/`[[ ]]`/heredoc 等 bash 语法）；危险命令（rm -rf 根目录、pkill、磁盘操作、关机等）默认被 BLOCK 拦截，`force=true` 可绕过；拦截采用**命令位锚定**——引号内/文档中的引用性文本不误拦，`sudo mkfs`/`shutdown` 等带前缀写法也能兜住；`timeout` 可调（1~3600s）；大输出（≥3K）自动落盘，返回头部+尾部+stderr 摘要预览 |
| `read_file` | `file_path: str, offset?: int, limit?: int` | 读取文件，支持行范围 |
| `write_file` | `file_path: str, content: str` | 创建或覆写文件 |
| `edit_file` | `file_path: str, old_string: str, new_string: str` | 精确字符串替换（首次出现） |

`tools/core.py` 中每个工具定义包含：

1. **OpenAI schema** —— 描述参数结构
2. **异步执行函数** —— `async def execute_xxx(params) -> str`
3. **统一分发入口** —— `execute_core_tool(name, params)` 负责路由

```python
# tools/core.py 结构示意
TOOL_DEFINITIONS = [
    {
        "type": "function",
        "function": {
            "name": "bash",
            "description": "执行 shell 命令；危险命令默认被安全检查拦截，force=true 可绕过",
            "parameters": {
                "type": "object",
                "properties": {
                    "command": {"type": "string", "description": "命令内容"},
                    "timeout": {"type": "integer", "description": "超时秒数（1~3600，默认 300）"},
                    "force": {"type": "boolean", "description": "跳过副作用检查（危险命令放行）"}
                },
                "required": ["command"]
            }
        }
    },
    # ...read_file, write_file, edit_file
]

async def execute_core_tool(name: str, params: dict) -> str:
    """路由到对应实现"""
```

---

## 插件工具的两种模式

### 模式 A：单工具模式（11/12 个插件使用）

**约定：** `PLUGIN_DEFINITION` + `execute(params) -> str`

```python
PLUGIN_DEFINITION = {
    "type": "function",
    "function": {
        "name": "web_search",
        "description": "快速搜索...",
        "parameters": { ... }
    }
}

async def execute(params: dict) -> str:
    query = params["query"]
    ...
    return str(results)
```

内部存储 key = `plugin_name`（文件名去掉 `_plugin.py`）

### 模式 B：多工具模式（目前只有 kdeconnect_plugin 使用）

**约定：** `PLUGIN_DEFINITIONS`（列表）+ `TOOL_MAP`（`{tool_name: executor}`）

```python
PLUGIN_DEFINITIONS = [
    {"type": "function", "function": {"name": "kdeconnect_notify", ...}},
    {"type": "function", "function": {"name": "kdeconnect_share_text", ...}},
    {"type": "function", "function": {"name": "kdeconnect_send_screenshot", ...}},
]

TOOL_MAP = {
    "kdeconnect_notify": _exec_notify,
    "kdeconnect_share_text": _exec_share_text,
    "kdeconnect_send_screenshot": _exec_screenshot,
}
```

内部存储 key = `f"{plugin_name}/{tool_name}"`（如 `"kdeconnect/kdeconnect_notify"`）

**两种模式的判断顺序：** 先检查多工具模式（`PLUGIN_DEFINITIONS` + `TOOL_MAP`），再检查单工具模式（`PLUGIN_DEFINITION` + `execute`）。

---

## ToolRegistry 类

```python
class ToolRegistry:
    def get_all_definitions(self) -> list[dict]:
        """合并核心 + 插件工具定义（核心在前，插件在后）"""

    async def execute(self, tool_name: str, params: dict) -> Any:
        """路由到核心或插件执行器"""
```

### 执行路由逻辑

```python
async def execute(self, tool_name, params):
    # 1. 核心工具直接路由
    if tool_name in {"bash", "read_file", "write_file", "edit_file"}:
        return await self._core_executor(tool_name, params)
    # 2. 遍历 _plugins values() 匹配 definition["function"]["name"]
    for plugin_data in self._plugins.values():
        if def_name == tool_name:
            return await plugin_data["executor"](params)
    raise ValueError(f"未知工具: {tool_name}")
```

---

## 同名覆写机制

### 加载顺序驱动的覆盖

```python
def _load_plugins(self):
    self._load_from_dir(builtin_dir, ...)  # 先内置
    self._load_from_dir(user_dir)           # 后用户 → 同名覆盖
```

内置插件先写入 `_plugins` dict，用户插件后写入 —— **同名 key 直接覆盖**。

### 覆写能力边界

| 类别 | 可覆写？ | 原因 |
|------|----------|------|
| 核心工具（bash/read_file/write_file/edit_file） | ❌ 不可 | 通过独立 `_core_executor` 分发 |
| 内置插件 | ✅ 可 | 用户目录同名文件覆盖 |
| 用户自定义新工具 | ✅ 可 | 无白名单限制 |

### 实际案例：web_search 覆写

用户 `~/.local/share/fp/private/tools/extensions/web_search_plugin.py` 与内置同名，其 `PLUGIN_DEFINITION` 中的 description 增加了"如果需要综合分析多篇文章的内容，请用 smart_web_search"的提示，这是降级到 smart_web_search 的引导。

---

## 用户目录加载

```python
# 内置目录（包路径导入）
module = importlib.import_module(f".extensions.{plugin_name}", package="fp_core.tools")

# 用户目录（文件路径导入）
spec = importlib.util.spec_from_file_location(plugin_name, filepath)
module = importlib.util.module_from_spec(spec)
spec.loader.exec_module(module)
```

- **用户目录：** 三来源 `~/.local/share/fp/{private,public,fetched}/tools/extensions/`（XDG 标准，优先级 private > public > fetched）
- **扫描条件：** 文件名以 `_plugin.py` 结尾，按字母序排序

---

## 全局单例 vs 独立实例

### 全局单例

```python
registry = ToolRegistry()  # 模块加载时自动初始化
```

`dispatch()`、`execute_tool()` 以及快捷函数共享此实例。

### 独立实例工厂（新架构，推荐）

```python
def create_registry() -> ToolRegistry:
    return ToolRegistry()  # 全新实例，重新扫描
```

`ToolExecutor` 默认使用 `create_registry()`，支持多 Agent 隔离。

---

## ToolExecutor 封装

```python
class ToolExecutor:
    def __init__(self, registry=None):
        self._registry = registry or create_registry()  # 默认独立实例

    def get_definitions(self) -> list[dict]:
        return self._registry.get_all_definitions()

    async def execute(self, tool_call: dict) -> str:
        name = tool_call["function"]["name"]
        args = json.loads(tool_call["function"]["arguments"])
        result = await self._registry.execute(name, args)
        return str(result)
```

**三层封装：**

1. **输入适配** —— 从 LLM tool_call 中提取 name + args
2. **输出适配** —— 任意类型转为 str
3. **错误隔离** —— 异常由框架统一捕获处理

---

## 异步快捷函数

```python
async def bash(command: str) -> str
async def read_file(file_path, offset=None, limit=None) -> str
async def write_file(file_path, content) -> str
async def edit_file(file_path, old_string, new_string) -> str
async def python(code: str) -> str
async def web_search(query: str) -> str
```

---

## 内置插件工具列表

内置插件工具共 **10 个**（与 4 个核心工具合计 14 个），来自两种注册途径：

**① `tools/extensions/` 静态扩展（5 个）**

| 工具名 | 插件文件 | 功能 |
|--------|----------|------|
| `web_fetch` | `web_fetch_plugin.py` | 抓取网页内容 |
| `python` | `python_plugin.py` | 执行 Python 代码 |
| `subagent` | `subagent_plugin.py` | 派遣子 agent |
| `memory_read` | `memory_read_plugin.py` | 读取长期记忆（索引行 `name→hint` 内联短硬事实；精确读取附创建/更新时间，超 30 天未更新给 ⚠️ 过期提醒） |
| `memory_save` | `memory_save_plugin.py` | 保存长期记忆（可选 `hint` 参数写入 frontmatter；每次保存刷新 `updated` 并保留原 `created`） |

**② 生命周期插件 ON_INIT 动态注册（5 个）**

| 工具名 | 来源插件 | 功能 |
|--------|----------|------|
| `task_create` | `plugins/task_system/` | 创建任务跟踪 |
| `task_update` | `plugins/task_system/` | 更新任务状态（pending/in_progress/delivered/completed/superseded） |
| `task_list` | `plugins/task_system/` | 列出任务 |
| `task_clear` | `plugins/task_system/` | 清除终态任务（已完成 + 已作废；delivered 不清） |
| `shortcircuit` | `plugins/shortcircuit/` | 短路压缩连通块 |

动态注册工具键形如 `lifecycle/<name>`，与静态扩展（目录文件）不同——它没有
"文件即开关"的生命周期，**注册表不会自动回收**：插件禁用
（`PluginRegistry.unregister → plugin.on_unregister()`）时须由插件自行调用
`ToolRegistry.unregister_tool(name)` 成对清理（task_system 清 4 个 task_*、
shortcircuit 清 `shortcircuit`），否则残留工具仍会被 `execute()` 按名匹配调用。
`register_tool` / `unregister_tool` 是对称 API，仅作用于 `lifecycle/` 键。

> 用户可通过三来源用户工具目录（`~/.local/share/fp/{private,public,fetched}/tools/extensions/`）新增/覆写工具，文件即开关，见 [扩展工具.md](../self/扩展工具.md)。

---

## 用户级工具示例（本机部署环境，非仓库内置）

> 以下为**当前开发机的用户目录工具**（`~/.local/share/fp/private/tools/extensions/`，老位置 tools/plugins/ 已并入 extensions），用于演示"用户级扩展"能力；**其他部署环境不保证存在**，切勿当作项目内置能力引用。

| 工具名 | 插件文件 | 说明 |
|--------|----------|------|
| `web_search` | `web_search_plugin.py` | 网络搜索（当前已 `.disabled`） |
| `codegraph_query` | `codegraph_query_plugin.py` | 代码结构查询 |
| `smart_web_search` | `smart_web_search_plugin.py` | AI 联网搜索总结 |
| `vision` | `vision_plugin.py` | 图像识别 |
| `kdeconnect_notify` | `kdeconnect_plugin.py` | 多工具：发送通知 |
| `kdeconnect_share_text` | `kdeconnect_plugin.py` | 多工具：分享文本 |
| `kdeconnect_send_screenshot` | `kdeconnect_plugin.py` | 多工具：发送截图 |

---

## 插件开发规范

### 单工具模式示例

```python
"""tools/extensions/my_tool_plugin.py"""

PLUGIN_DEFINITION = {
    "type": "function",
    "function": {
        "name": "my_tool",
        "description": "示例工具——将输入翻转",
        "parameters": {
            "type": "object",
            "properties": {
                "input": {
                    "type": "string",
                    "description": "要翻转的文本"
                }
            },
            "required": ["input"]
        }
    }
}

async def execute(params: dict) -> str:
    """推荐异步实现；错误时 raise Exception。"""
    text = params["input"]
    return text[::-1]
```

### 多工具模式示例

```python
"""tools/extensions/my_multi_plugin.py"""

PLUGIN_DEFINITIONS = [
    {
        "type": "function",
        "function": {
            "name": "tool_a",
            "description": "...",
            "parameters": { ... }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "tool_b",
            "description": "...",
            "parameters": { ... }
        }
    },
]

TOOL_MAP = {
    "tool_a": _exec_tool_a,
    "tool_b": _exec_tool_b,
}

async def _exec_tool_a(params: dict) -> str:
    ...

async def _exec_tool_b(params: dict) -> str:
    ...
```

### 规则

- `execute` / 各 executor 必须返回 `str`（LLM 接收的是字符串）
- 推荐异步（`async def`），同步会自动包装
- 发生错误时 `raise Exception`，由框架统一捕获并返回错误消息
